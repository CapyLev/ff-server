version: '3.9'

services:
  nginx:
    build:
      context: ./nginx
      dockerfile: ./Dockerfile
      target: prod
    container_name: nginx
    restart: always
    ports:
      - '80:8080'
    networks:
      - ff-network

networks:
  ff-network: